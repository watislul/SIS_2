# SIS_2

# Manga ETL Pipeline — Полный README

Этот проект представляет собой полноценный ETL-конвейер для сбора, очистки и загрузки данных о манге с сайта **Remanga.org**.  
Пайплайн полностью автоматизирован и управляется через **Apache Airflow**, а запуск возможен как через веб-интерфейс Airflow, так и вручную.

---

## 📌 Цели проекта

- автоматически собрать актуальные данные о популярной манге  
- очистить информацию от мусора, дубликатов и обрывов  
- привести данные к единообразному формату  
- загрузить всё в локальную базу SQLite  
- убедиться, что результат содержит ≥100 корректных записей  
- показать рабочий ETL-процесс с Airflow + Docker

---

## ⚙️ Как работает пайплайн

Пайплайн состоит из трёх основных этапов:

### **1. Скрапинг (scraper.py)**

С помощью Selenium в headless-режиме скрипт:

- открывает каталог манги на remanga.org  
- собирает до 120 ссылок на популярные тайтлы  
- переходит по каждой странице и извлекает:
  - название  
  - описание (обрезает до 500 символов)  
  - рейтинг  
  - год  
  - ссылку на обложку  
  - уникальный manga_id  
  - сохранённый timestamp  

Все данные сохраняются в `data/raw_manga.json`.

---

### **2. Очистка данных (cleaner.py)**

Скрипт проводит полноценную нормализацию:

- удаляет дубли по `url`
- исправляет обрезанные описания (`fix_truncated_description`)
- убирает лишние кавычки и пробелы
- приводит заголовки к нормальной форме
- конвертирует типы (`rating`, `year`, `scraped_at`)
- фильтрует записи с пустыми названиями
- создаёт поле `has_cover`
- извлекает `manga_id`
- сохраняет результат в:

  - `cleaned_manga.json`  
  - `cleaned_manga.csv`

Также выводится статистика очистки.

---

### **3. Загрузка в базу (loader.py)**

Скрипт:

- создаёт таблицу `manga` (если нет)
- вставляет или обновляет записи через `INSERT OR REPLACE`
- выводит количество загруженных тайтлов

База находится в `data/output.db`.

---

## 🛠 Airflow DAG

DAG `manga_final` состоит из четырёх задач:

1. **test_airflow**  
   Проверяет, что Airflow работает корректно.

2. **check_files**  
   Проверяет наличие всех .py файлов, необходимых пайплайну.

3. **run_pipeline**  
   Запускает по очереди:
   - scraper.py  
   - cleaner.py  
   - loader.py  

4. **final_check**  
   Читает SQLite-базу и выводит количество записей в таблице `manga`.  
   Должно быть ≥100 для успешного результата.

---

## 🚀 Как запустить проект

### 1. Запуск через Docker
docker-compose up –build


После старта Airflow доступен по адресу:

http://localhost:8080  
Логин: **admin**  
Пароль: **admin**

В интерфейсе Airflow запустите DAG `manga_final`.

---

### 2. Ручной запуск (без Airflow)
python src/scraper.py
python src/cleaner.py
python src/loader.py


---

## 📊 Ожидаемый результат

После успешного выполнения пайплайна:

- в `data/raw_manga.json` — сырые данные  
- в `data/cleaned_manga.json` — очищенные данные  
- в `data/output.db` — база SQLite с таблицей `manga`  
- в логах Airflow видно:

Таблица ‘manga’: 100+ записей
ПАЙПЛАЙН ЗАВЕРШЕН!

---

## 🎯 Что демонстрирует проект

- веб-скрапинг динамических страниц через Selenium  
- устойчивость к изменению структуры HTML  
- продуманная очистка текстов  
- работающий ETL-процесс  
- использование Airflow для оркестрации задач  
- развёртывание через Docker  
- минимальная, но полноценная база данных  

Проект подходит как учебный, курсовой, лабораторный или портфолио-проект по Data Engineering.

---

